// Entry point — orchestrates startup, feature registration, and hands off to eframe.
// See docs/ARCHITECTURE.md for the full startup sequence.
fn main() {
    eprintln!("myterm workspace skeleton — implementation pending (see agent-docs/plans/)");
}
